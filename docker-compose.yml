version: '3.8'
services:
  agent:
    build: .
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - MODEL=${MODEL:-qwen/qwen3-coder:free}
    volumes:
      - ./workspace:/workspace
      - ./logs:/app/logs
      - ./memory:/app/memory
    stdin_open: true
    tty: true
